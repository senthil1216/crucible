[pytest]
# Only collect the project's own test suite. The agent writes generated
# solution + pytest files into tmp/ (and runs throwaway suites in temp dirs);
# those must never be picked up by the project's test run.
testpaths = tests
norecursedirs = tmp workspace test_workspace .venv .git .agent_state .agent_memory .pytest_cache
