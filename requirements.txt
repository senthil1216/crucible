# Core dependencies (required for basic agent operation + Ollama + semantic memory)
openai>=1.0.0
httpx>=0.24.0

# Required for semantic memory (long-term + failure memory similarity)
# Default model: all-MiniLM-L6-v2 (~80 MB, CPU-friendly)
sentence-transformers>=2.2.0

# Optional: For cloud LLM APIs (OpenAI, Anthropic, etc.)

# Optional: For Anthropic integration  
# anthropic>=0.20.0

# Optional: For llama.cpp local models
# llama-cpp-python>=0.2.0

# Optional: Docker-based sandbox executor (recommended for real dependency support)
# docker>=7.0.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Required: the agent gates success on a real pytest run and parses its
# machine-readable JSON report (used by agent/pytest_report.py).
pytest-json-report>=1.5.0
